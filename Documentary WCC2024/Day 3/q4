def findTheWinner(n: int, k: int) -> int:
    winner = 0  # Josephus position for 1 person
    for i in range(2, n + 1):
        winner = (winner + k) % i
    return winner + 1  # Convert from 0-indexed to 1-indexed
